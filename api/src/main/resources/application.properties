spring.application.name=api
server.port=${PORT:8080}

# Neon PostgreSQL Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:${DATABASE_URL:jdbc:postgresql://ep-crimson-water-axitntoo-pooler.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_7ue6LkRhHIMO}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

